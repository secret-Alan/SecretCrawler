# Web Crawler with PySide6 UI - Python dependencies
#
# Install all dependencies with:
#     pip install -r requirements.txt
#
# The compatible-release operator (~=) is used so that patch updates are
# allowed while keeping the documented minor/major API surface stable.

PySide6 ~= 6.5
# QtWebEngine 随 PySide6 安装包提供，import PySide6.QtWebEngineWidgets 即可使用
requests ~= 2.31
beautifulsoup4 ~= 4.12
lxml ~= 5.0
openpyxl ~= 3.1
PySocks ~= 1.7
pyqtgraph ~= 0.13
Pillow ~= 10.0
markdown ~= 3.5
